#include <bits/stdc+.h>
using namespace std;
const int maxn=100005;
int len[maxn],value[maxn];
int n;

int main(int argc, char const *argv[])
{
	scanf("%d",&n);
	for(int i=0;i<n;i++)
		scanf("%d %d",&len[i])	
	return 0;
}
