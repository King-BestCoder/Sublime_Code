#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <iostream>
#include <string>
#include <algorithm>
#include <set>
#include <vector>
#include <map>
using namespace std;
int n;
int num[105];
int dp[105];
int main(int argc, char const *argv[])
{
	int T;
	scanf("%d",&T);
	while(T--){
		memset(num,0,sizeof(num));
		memset(dp,0,sizeof(dp));
		scanf("%d",&n);
		for(int i=0;i<n;i++){
			scanf("%d",&num[i]);
		}
		for(int i=0;i<n;i++){
			
		}
	}
	return 0;
}
