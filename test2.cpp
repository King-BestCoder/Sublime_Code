#include <iostream>
#include <string>
#include <vector>
#include <algorithm>
#include <sstream>

using namespace std;

int main()
{
    string line,pass;
    cin>>pass;
    getline(cin,line);
    int n=pass.length();
    string enc;
    for(int i=0;i<line.length();i++){
        if(isalpha(line[i])){
            
        }
    }
    return 0;
}
